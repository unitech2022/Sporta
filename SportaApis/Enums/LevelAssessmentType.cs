namespace SportaApis.Enums;

public enum LevelAssessmentType
{
    SelfAssessment = 0, // المسار 1 - أسئلة ذاتية
    VideoUpload = 1,    // المسار 2 - رفع فيديو
    CoachSession = 2    // المسار 3 - جلسة مع مدرب معتمد (مدفوع)
}
